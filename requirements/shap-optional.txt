# Optional for official TreeExplainer output in the main SHAP runner.
# If this package is not installed, the runner falls back to XGBoost
# pred_contribs=True, which still returns Tree SHAP contributions:
#   python scripts/run_shap_sdg16_vietnam.py
#
# On Windows + Python 3.13, shap may try to compile from source and require
# Microsoft C++ Build Tools. Prefer Python 3.11/3.12 if you really need this.
shap==0.46.0
